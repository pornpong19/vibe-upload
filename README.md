# Vibe Upload - YouTube Video Uploader

โปรแกรมสำหรับอัพโหลดวิดีโอขึ้น YouTube ด้วย Electron และ YouTube API

## คุณสมบัติ

- ✅ อัพโหลดวิดีโอขึ้น YouTube
- ✅ รองรับหลายช่อง YouTube
- ✅ ตั้งค่ารายละเอียดวิดีโอ (ชื่อ, คำอธิบาย, แท็ก, หมวดหมู่)
- ✅ เลือกสถานะการเผยแพร่ (Public, Unlisted, Private)
- ✅ ตั้งเวลาเผยแพร่ล่วงหน้า (Scheduled Publish)
- ✅ ตั้งค่า Made for Kids
- ✅ แสดงความคืบหน้าการอัพโหลดแบบเรียลไทม์
- ✅ จัดการช่อง YouTube หลายช่อง
- ✅ แสดงสถิติช่อง (ผู้ติดตาม, จำนวนวิดีโอ, การดู)
- ✅ รีเฟรชข้อมูลช่องได้ทันที
- ✅ ออกแบบ UI สไตล์มินิมอล โทนสีแดง-ขาว

## โครงสร้างโปรเจค

```
vibe-upload/
├── main.js                 # Electron main process
├── preload.js             # Preload script
├── package.json           # Package configuration
├── pages/                 # HTML pages
│   ├── index.html        # หน้าแรก (อัพโหลดวิดีโอ)
│   └── channels.html     # หน้าจัดการช่อง
├── src/                   # Backend modules
│   ├── channels-manager.js   # จัดการช่อง YouTube
│   └── upload-handler.js     # จัดการอัพโหลดวิดีโอ
├── renderer/              # Frontend scripts
│   ├── index-renderer.js     # Script หน้าแรก
│   └── channels-renderer.js  # Script หน้าจัดการช่อง
├── styles/                # CSS files
│   └── main.css          # Main stylesheet
└── assets/                # Static assets
    └── icon.png          # App icon
```

## การติดตั้ง

1. ติดตั้ง dependencies:
```bash
npm install
```

2. รัน application:
```bash
npm start
```

## การตั้งค่า YouTube API

> 📖 **อ่านคู่มือการตั้งค่าแบบละเอียด**: [SETUP_GUIDE.md](SETUP_GUIDE.md)

### สรุปขั้นตอนสั้นๆ:

1. **สร้างโปรเจคใน Google Cloud Console**
   - ไปที่ [Google Cloud Console](https://console.cloud.google.com/)
   - สร้างโปรเจคใหม่
   - เปิดใช้งาน **YouTube Data API v3**

2. **ตั้งค่า OAuth Consent Screen**
   - เลือก External
   - เพิ่มตัวเองเป็น Test user

3. **สร้าง OAuth 2.0 Credentials**
   - สร้าง OAuth client ID (Desktop app)
   - **สำคัญ!** เพิ่ม Redirect URI: `http://localhost:3000`
   - ดาวน์โหลด credentials.json

4. **เพิ่มช่องใน Vibe Upload**
   - อัพโหลด credentials.json
   - เบราว์เซอร์จะเปิดให้ยืนยันตัวตนอัตโนมัติ
   - ยืนยันและอนุญาตการเข้าถึง
   - ช่องจะปรากฏในโปรแกรมทันที

## การใช้งาน

### อัพโหลดวิดีโอ

1. **เลือกวิดีโอ**: คลิกที่พื้นที่อัพโหลดเพื่อเลือกไฟล์วิดีโอ
2. **กรอกรายละเอียด**:
   - ชื่อวิดีโอ (บังคับ)
   - คำอธิบาย (ไม่บังคับ)
   - แท็ก (ไม่บังคับ, คั่นด้วยจุลภาค)
   - หมวดหมู่
   - สถานะการเผยแพร่ (Public/Unlisted/Private)
   - ตั้งเวลาเผยแพร่ล่วงหน้า (ไม่บังคับ)
   - Made for Kids (เลือกหรือไม่เลือก)
3. **เลือกช่อง**: เลือกช่อง YouTube ที่ต้องการอัพโหลด
4. **คลิกอัพโหลด**: กดปุ่ม "อัพโหลดวิดีโอ"

### จัดการช่อง

- **เพิ่มช่อง**: อัพโหลด credentials.json และทำการเชื่อมต่อ
- **ดูสถิติช่อง**: แสดงผู้ติดตาม, จำนวนวิดีโอ, และการดูทั้งหมด
- **รีเฟรชข้อมูล**: คลิกปุ่ม 🔄 เพื่ออัพเดทสถิติล่าสุด
- **ลบช่อง**: คลิกปุ่ม "ลบ" ที่ช่องที่ต้องการ

## ข้อจำกัด

- ขนาดไฟล์สูงสุด: 256GB
- รูปแบบไฟล์ที่รองรับ: MP4, MOV, AVI, MKV, WEBM
- โควต้า YouTube API: 10,000 units ต่อวัน (1 การอัพโหลด = 1,600 units)

## หมายเหตุ

- ไฟล์ credentials.json ของแต่ละช่องจะถูกเก็บแยกกันใน `userData` directory
- โทเค็นการเข้าถึง (tokens) จะถูกเก็บไว้สำหรับการใช้งานในครั้งถัดไป
- หากโทเค็นหมดอายุ จะต้องทำการเชื่อมต่อช่องใหม่

## License

MIT
